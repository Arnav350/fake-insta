import { StyleSheet, Text, View } from "react-native";

const Reels = () => {
  return (
    <View>
      <Text>Reels</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Reels;
