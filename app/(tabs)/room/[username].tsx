import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Room = () => {
  return (
    <View>
      <Text>Room</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Room;
